import { Request, Response, NextFunction } from "express";

export const getRestaurantsController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const getRestaurantByIdController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const getRestaurantMenuController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const getRestaurantReviewsController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};