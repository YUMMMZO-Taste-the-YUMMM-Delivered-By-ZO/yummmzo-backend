import { Request, Response, NextFunction } from "express";

export const getAddressesController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const getAddressByIdController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const createAddressController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const updateAddressController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const deleteAddressController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};

export const setDefaultAddressController = async ( req: Request, res: Response, next: NextFunction ): Promise<void> => {
    // logic...
};